import React from 'react';

// Import Styled Components
import {
    HeaderWrapper,
    HeaderTitle
} from './Header.styles';

const Header = () => {
    return (
        <HeaderWrapper>
            <HeaderTitle>SoundCloud App</HeaderTitle>
        </HeaderWrapper>
    )
}

export default Header; 
